# Drizzle
DATABASE_URL=local.db

# OpenAI (optional - AI features disabled when unset)
OPENAI_KEY=

# SMTP - required for magic link / PIN login emails
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Admin email - only this address may use the flashcard repair endpoint
ADMIN_MAIL=
